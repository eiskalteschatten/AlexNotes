export interface DatabaseConfigInterface {
    storage: string;
    name: string;
}

export interface DefaultUserInterface {
    firstName: string;
    lastName: string;
    username: string;
    password: string;
    emailAddress: string;
}
